# # Trigger when the switch is toggled.
# bindl = , switch:[switch name], exec, swaylock
# # Trigger when the switch is turning on.
# bindl = , switch:on:[switch name], exec, hyprctl keyword monitor "eDP-1, disable"
# # Trigger when the switch is turning off.
# bindl = , switch:off:[switch name], exec, hyprctl keyword monitor "eDP-1, 2560x1600, 0x0, 1"

# "Lenovo Yoga Tablet Mode Control switch"
# "Lid Switch"
bind = , switch:on:Lenovo Yoga Tablet Mode Control switch, exec, notify-send "Mode changed" "Now in tablet mode"
bind = , switch:off:Lenovo Yoga Tablet Mode Control switch, exec, notify-send "Mode changed" "Now in laptop mode"

# bind = , switch:on:Lenovo Yoga Tablet Mode Control switch, exec, $NH_FLAKE/scripts/hyprland/waydroid-fullscreen.sh
# bind = , switch:off:Lenovo Yoga Tablet Mode Control switch, fullscreenstate, 0 0 
